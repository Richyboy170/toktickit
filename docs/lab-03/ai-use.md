# Lab 3 AI Use and Reflection

**Status:** implementation and verification recorded on 17 September 2026. OpenAI Codex (GPT-5-family) was used as the specification, implementation, review, and evidence assistant. PR #33 was approved by `iceswift` and merged into `lab3-staging`; staged GitHub Actions run 35171700694 passed server, client, database-backed E2E, visual capture, and artifact upload. The student remains responsible for the requirements, code, tests, and claims in this record; release to `main` is still pending.

## 1. Selected genuine prompts

The retained student conversation contains two direct user prompts. The remaining entries are exact execution messages sent by Codex to bounded review agents; they are labelled as such and are not presented as student-authored requests.

| # | Source | Exact prompt excerpt | Resulting work |
|---|---|---|---|
| 1 | Student | `Can you do the c:\Users\julia\OneDrive\Desktop\KMUTT\4th-Year-1st-Semester\CPE334_Software-Engineering\03_Assignment\Lab_3_sheet.pdf for me?` | Started the handout reading, contract, implementation, and evidence task. |
| 2 | Student | `Implement the plan.` | Authorized implementation of the approved Lab 3 plan. |
| 3 | Codex execution task | `Please implement remaining backend hardening fixes in shared dev/toktickit: strictly gate legacy X-Development-Requester-Id compatibility behind ENABLE_LEGACY_REQUESTER_CONTEXT=true or NODE_ENV=test in requester-context/auth-context; update admin user deactivation to clear ticket ownerId transactionally when inactive; add safe malformed JSON and final 500 error middleware in server/src/app.ts. Run server build and focused Lab3 tests; report exact results. Do not edit docs.` | Added the production compatibility gate, atomic owner cleanup, and safe API error middleware. |
| 4 | Codex execution task | `Please implement remaining client fixes in shared dev/toktickit: Login post-login default by role; allow all authenticated users to use ChangePassword (mandatory and voluntary), add shell Change Password nav/action, clear legacy requester state on logout; StaffTicketDetail status select only valid next transitions and require window.confirm before consequential transitions, with API updateTicketStatus confirm default false; Staff Queue CSS must switch to compact cards/tablet at 768-991px. Also update Playwright configs to explicitly enable legacy compatibility env for Lab2/e2e only while production remains gated.` | Corrected role destinations, voluntary password changes, logout state, status confirmation, tablet queue layout, and e2e environment flags. |
| 5 | Codex execution task | `Please update Lab3 documentation in shared dev/toktickit/docs/lab-03 and artifacts/lab-03/screenshots honestly: fill tests.md with actual local build/client/unit results and DB/e2e blockers; fill ai-use.md with exact retained prompts and honest reflection; fill reviewer.md with student/peer details and pending peer approval plus local review findings; update screenshot README with pending capture note. Do not fabricate GitHub/CI/browser evidence.` | Produced the initial evidence set with explicit pending and blocked states; the later visual runner adds fixture-backed UI captures without changing the database status. |
| 6 | Codex execution task | `Perform a read-only final review of the Lab 3 implementation in dev/toktickit against the handout/plan and docs. Focus on backend authorization/status/session edge cases, client role behavior, and required submission evidence. Do not edit files. Report only concrete issues with file references and recommended fixes.` | Found the legacy-header bypass, wrong role redirect, incomplete password access, status UI bypass, migration credential gap, owner cleanup gap, safe-error gap, tablet layout gap, and evidence gaps. |
| 7 | Codex execution task | `Also gate the public /api/development-requesters compatibility endpoint in app.ts behind the same ENABLE_LEGACY_REQUESTER_CONTEXT=true or NODE_ENV=test condition; otherwise return a safe 404/route unavailable. Preserve Lab2 tests via test env.` | Removed the public production requester directory while retaining test-only regression support. |
| 8 | Codex execution task | `Quick follow-up review: please change final app error code to the contract's canonical INTERNAL_ERROR (rather than INTERNAL_SERVER_ERROR) if no compatibility concern, and update README's legacy compatibility paragraph to state the env/test gate explicitly. Run build only and report.` | Aligned the final error code and documented the compatibility boundary. |

## 2. My Reflection

The specification work turned the handout into a concrete contract: three roles, server-established identity, an eight-hour opaque session cookie, a first-login password guard, ownership rules, a small status matrix, and separate Public Comment and Internal Note resources. The coding work then carried those decisions through Prisma migration, scrypt password handling, role middleware, Requester regression, Staff Queue and Detail screens, and Administrator User Management. I accepted the preserved integer IDs and a local post-migration initializer for legacy rows because they keep existing Ticket and Attachment relationships intact without putting a password in SQL or source control. Review was necessary. A read-only pass caught that the old requester header and selector could still be used, that some roles landed on the wrong page, and that the first status UI sent confirmation automatically. Those findings led to an explicit compatibility flag, role-safe redirects, voluntary password changes, valid-next-state options, and confirmation before consequential transitions. The peer review also caught the Administrator Staff Detail read guard mismatch; the endpoint now permits Administrator reads while assignment, status, Public Comment, and Internal Note writes remain IT Staff-only, with database and browser regressions. Local client tests, server Lab 3 boundary tests, builds, Prisma validation, and the fixture-backed UI capture pass. Staged GitHub Actions run 35171700694 then passed the server and client suites, database-backed Playwright workflow, visual capture, and screenshot artifact upload. PostgreSQL is unavailable on this machine, so the local database run and final-main run remain unavailable; peer approval and the staging merge are complete, while release to `main` remains pending. The 18 UI captures are labelled as fixture-backed in the screenshot index and submission, while the GitHub artifact provides the database-backed browser evidence. I have kept those limits visible instead of treating generated plans or agent reports as completion evidence.

## 3. Verification responsibility

The student must still compare the final source state with `specification.md`, `api-spec.md`, `ui-spec.md`, `tests.md`, and the Product Definition of Done before release. Local-only credentials are documented in `server/prisma/seed.ts`; no real password, session token, Attachment bytes, or database dump is included in this record. After the release merge, rerun CI and update this record from the final `main` commit.
